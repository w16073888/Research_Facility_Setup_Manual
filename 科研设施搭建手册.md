---
tags: [索引, 说明书, 方法论]
---

# 科研设施搭建手册

> **这份文档是给「另一个会话」的**：整篇喂过去，填三个空，就能在任意项目上搭出同构的设施。
>
> 它记的是 `D:\大学编程学习\obsidian` 这套设施**为什么长这样**（含踩过的坑），**不是**这套设施的说明书——说明书见库根 `README.md`。
>
> 读法：§0 是给接收方的指令，§1 是全文的论证基础，§2–§7 是配方，§8 是分档建议（**先读它，别全上**），§9 是 checklist，§10 是坑，**§11 是 skill 种子全文（新建项目时照它落盘）**。

## 0. 怎么用这份文档（给接收方的指令）

接收方需要填三个空：

| 空 | 是什么 | 影响 |
|---|---|---|
| `{{项目根}}` | 设施装在哪 | — |
| `{{规模档}}` | S / M / L（见 §8） | 决定要不要机器生成区、要不要 skill、要不要 `ROADMAP` |
| `{{不可写区}}` | 哪些目录是只读 / 外部的 | 决定树的**边界**与自检的**豁免表** |

填完按 §9 的 checklist 从 1 做到 8。

**一句话目标**：新会话（或三个月后的你）能在 5 分钟内知道**这个项目有什么、现在到哪了、该读哪一份**。

## 1. 为什么要这套东西：三条失败模式

设施不是"文档写得好"，是对三个**具体**失败模式的回答。理解了它们，才知道哪条规则承重、哪条可以砍。

| # | 失败模式 | 症状 | 回答在哪 |
|---|---|---|---|
| 1 | **静默腐化** | 清单落后于磁盘，**没有任何东西报警** | §3 单一索引 + §5 自检闸 |
| 2 | **入口缺失** | 文档都在，新会话不知道从哪进 | §2 根 README + §7 那一句话 |
| 3 | **靠人记住的约定会失效** | "记得回写"在第三个月失效 | §4 写成步骤 + §6 skill 化 |

**#1 是根本原因，也是唯一会自动恶化的一条。** 文档不会因为写得好而保持好，它只会因为**没人改它**而变坏——所以这套设施里最值钱的是 §5 那个几十行的脚本，不是任何一份文档。

## 2. 树的形状

三层，节点类型只有两种：

```
根 README.md          ← 总目录。回答"有哪些块"
  ├── 子 README.md    ← 每块一份。回答"这块有什么、现在到哪了"
  │     └── 叶子文件   ← 分两类：见下「计划文件 / 数据文件」
  └── 根目录散件       ← 不属任何块的文件，也在根 README 里登记
```

### 叶子文件分两类：计划文件 / 数据文件

**任何 md（和文件）先归到两类之一，README 按这个分，浏览也按这个读。**

| 类型 | 是什么 | 谁来读 | 例子 |
|---|---|---|---|
| **计划文件** | 记录**日志、经验、规划**、决策、状态的文件 | **新会话必须读**，浏览项目只读这一类 | `日志/`、`经验/`、`规划/`、设计文档、README |
| **数据文件** | 语料、产物、生成数据、构建结果——量大、可重建 | **新会话不读**（占 token 且失真） | 向量 `.npz`、聚类 json、数据集、缓存 |

> **每次浏览项目只读计划文件。** 数据文件不进上下文：要么能重建、要么该由脚本/工具读；人读只会灌 token、又被过大文件带偏。

### 根 README 模板（五节）

```markdown
---
tags: [索引, 说明书]
---

# <项目名> · 总目录

## 1. 索引树
| 文件夹 | 干什么 | 它的 README | 最后更新 |
|---|---|---|---|
| `A/` | <一句话用途> | [[A/README\|打开]] | YYYY-MM-DD |

## 2. <直辖文件夹的索引>     ← 见下「直辖」一节
## 3. 根目录的散件
## 4. 建议阅读顺序             ← 这一节决定新会话的头五分钟；**只列计划文件，不列数据**
## 5. 不在树上的目录           ← 每一项都要写"为什么不列"
## 6. 维护本文件（树形回写）    ← §4 的传播表，本文件那一列
```

### 子 README 模板（**子 README 之间必须同构**）

```markdown
## 1. 这个文件夹是什么
## 2. 当前工作状态        ← 问"现在到哪了"只读这一节
## 3. 文件清单            ← 分两段：**计划文件**（逐条列，且是唯一要读的）
                            ←          **数据文件**（记为"数据 ×N，可重建，不读"）
## 4. 建议阅读顺序        ← **只列计划文件**——新会话按这个读
## 5. 相关                ← 含**上位指针**（指回根，用纯文本路径）
```

⚠️ §3 与 §4 是配套的：**§3 标出"哪些是计划文件"，§4 就只排它们**。数据文件不出现 §4——否则"浏览只读计划文件"成了一句空话，又被几千个产物拖回原点。

**子 README 必须同构，根必须不同构**——根是地图，不含状态、不含叶子细节。

### ⚠️ 命名：子 README **不要**也叫 `README.md`

本库**每个文件夹的 README 连同根索引全叫 `README.md`**（本库真的这么干了，是个反面教材），
代价是两条硬约定：跨文件夹引用必须写全路径 `[[A/README|…]]`，且**根 README 不能入双链**。
> ⚠️ 别在这里写"总共几份"——**本库已因此栽过**：这个数加一个文件夹就过期一次，
> 而**过期的计数和"刚核对过"长得一模一样**。要数就现数。

**这是绕路，不是设计。** 复刻时子 README 叫 `_index.md` 或 `README-<folder>.md`——没有同名，根就能被正常双链，也少一条要靠人记住的约定。**任何靠人记住的约定，都会在第三个月失效。**

### ⚠️ 「直辖」文件夹

没有子 README 的文件夹（如 `日志/`、`参考/`、`工具/`），由根 README **直接管辖**——根里给它一个专属 §。

关键是**说清楚它没有子 README**，否则新会话会去找它。本库的写法：

> `| 日志/ | 研究日志——**没有子 README，由根 README 直接管辖**（索引在根 §2） |`

## 3. 单一索引原则

> **同一个事实只留一份，留在最不容易腐的那一层。**

本库实测：`论文趋势挖掘/README.md` 写着「日志（5 篇，格式见 `research-log` skill）」，而实际已有 **7 篇**。这不是笔误——是**同一个数被抄了两遍**，而它只在其中一处会更新。

| 数字 | 该放哪 |
|---|---|
| 有多少篇日志 | **根 README 里管辖日志的那一节**（它是日志唯一的索引） |
| 某个文件夹有多少**计划文件** | **那个文件夹的 README §3** |
| 某个文件夹有多少**数据文件** | **不逐条列**——只记为"数据 ×N，可重建"，别把几千个产物读进索引 |
| 全库总篇数、外部源可用性 | **只写一处**，别处只引用 |

判据：**问"这个数变了的时候，谁会知道？"** 如果答案是"没人"，就把它挪到唯一的那一处去。

## 4. 向上传播链（**本设施的核心**）

> **规则：改动任何 md 之后，顺着树往上写，终点是根 README。**

| 你改了什么 | 子 README | 根 README |
|---|---|---|
| 新建一个**计划文件** | §3 加一行 | **只改 §1 那一行的「最后更新」** |
| 删除 / 改名一个计划文件 | §3 删行 / 改行 | 同上 |
| 数据文件生成 / 体积变大 | **不动**（§3 那个"数据 ×N"一般不逐条登记） | **不动** |
| 改了工作状态、判据 | §2 改 | **不动** |
| 文件夹的**用途定位变了** | §1 改 | 改 §1 那一行的「干什么」 |
| 新建一篇日志 | — | 根里那一节加一行 |
| 根目录新建 / 删除散件 | — | §3 加 / 删一行 |
| 只改错别字、只动排版 | **不动** | **不动** |

> **根只吃「那一行」。它是地图，不是流水账。**
> 一旦开始记"这次改了什么"，根就会变成那种**"会让人重做已经做完的事"**的失真清单。

**由谁写：执行这次改动的那一次会话写。** 不是"以后有人整理"。

### 关于「最后更新」这一列

它是全表最容易腐的一列，而且是**唯一腐蚀了也看不出来**的一列——日期看起来永远合理。

两条路，选一条：

- **机器填**（推荐，§8 的 M / L 档）：从 git 时间或文件 mtime 现算
- **手填 + 被检查**：让 §5 的脚本查它有没有落后于文件 mtime 太多

> 本库的教训：一次会话里为了这一列手改了两处日期，而没有任何东西会告诉你漏了第三处。

## 5. 自检闸：树的承诺需要证明

「从根出发充分搜索就能读全」是一个**可测的断言**——既然可测，就必须测。

**为什么必须自动化**：树的失败模式是**静默**。没有东西会报错，它只是慢慢变得不全；而"变得不全"没有任何症状，直到三个月后有人（或某个新会话）照着它找东西，找不到。

| # | 查什么 | 为什么 |
|---|---|---|
| 1 | **悬空双链 = 0** | `[[X]]` 指向不存在的文件 |
| 2 | **无孤儿 md** | 每个 md 都能从根走到 ← **这条才是承诺的本体** |
| 3 | 含 md 的一级文件夹都有 README | 否则那一块根本没有入口 |

> ⚠️ **第 2 条最容易漏。** 本库第一版自检只查了第 1 条，结果**悬空 0、全绿**——而"读全"靠的恰恰是第 2 条。
> **只查一半的检查比没有检查更危险，因为它给出的是虚假的绿。**

### 铁律：不变量检查里不许有真实外网请求

本库的 `check_breaker.py` 原来有两条断言打真实站点，带宽被长任务挤掉时它们一起变红，报出来的却是「404 误触发熔断」——**失败信息指向网线，而不是被检查的代码**。

> **一个会误报的检查比没有检查更糟，它训练人忽略红色。**

所以自检脚本必须**离线、秒级、确定性**。任何需要网络的验证，改用本地假服务（`ThreadingHTTPServer` 跑在 `127.0.0.1:0`）。

### 脚本（直接抄，改 `CONFIG` 即可）

```python
#!/usr/bin/env python3
"""索引树自检：树还在不在。离线、秒级、确定性——不碰网络。

查三件事：
  1. 悬空双链      [[X]] 指向不存在的文件
  2. 孤儿 md       从根 README 出发走不到（"从根就能读全"的承诺，
                   这是它唯一能被证伪的地方）
  3. README 覆盖   每个含 md 的一级文件夹是否都有自己的 README

为什么必须自动化：树的失败模式是**静默**——没有东西会报错，它只是慢慢变得
不全，而"变得不全"没有任何症状。一个会误报的检查比没有检查更糟（它训练人
忽略红色），所以本脚本完全离线，结果确定。

2026-09-16 在真实库上跑第一版，**假阳性率 100%**——4 条"悬空"里 3 条是 skill
模板里的 `[[{{上一篇}}]]` 占位符、第 4 条在一条已划掉的待办里；248 条"孤儿"里
97 条是数据集、其余是根 README 已登记的散件。**所以：先跑，再按报告修配置，
别按想象修配置。** 每个修法都落成了配置项，见 CONFIG。

用法：
    python check_tree.py --vault . --data vault/papers --data dataset/clean
退出码：0 = 全绿，1 = 有问题。可直接挂 pre-commit。
"""

from __future__ import annotations

import argparse
import posixpath
import re
import sys
from collections import deque
from pathlib import Path

CONFIG = {
    # 树的根：可达性从这个文件算起
    "root_md": "README.md",

    # 子 README 的合法文件名（见手册 §2 命名一节）
    "readme_names": {"README.md", "_index.md"},

    # **数据**不是**文档**：不参与"从根可达"的检查，也**不被读取**。
    # 这是本脚本最重要的一项配置——不划清它，孤儿检查会被几千个数据文件
    # 淹没，然后所有人学会忽略这个脚本。
    # 规则：按**连续路径段**匹配，所以 ``vault/papers`` 能命中
    # ``a/vault/papers/x.md``，``cache`` 能命中任意层级下的 ``cache/``。
    "data_rules": [
        ".git", ".obsidian", "__pycache__", "node_modules", ".venv",
        "cache", "pdfs", "dist", "build",
        "vault/papers",          # 语料：是数据，不是文档
        "dataset/clean",         # 数据集：同上
        "dataset/raw",           # 数据集：同上
        ".claude",               # skill 与模板，根 README §5 已声明不在树上
    ],

    # 有意不进树的 md：相对路径 → 为什么它合法（**每条都要写原因**，
    # 否则这个表就是在掩盖问题）。已消失的条目会在报告里点出来。
    "exempt": {},

    # **按设计就没有子 README** 的一级文件夹（"直辖"文件夹，见手册 §2）。
    # 不登记它，检查会把设计报成缺陷——本库的 `日志/` 就被误报过。
    "no_readme_ok": {
        "日志": "直辖文件夹——根 README 直接管辖，索引在根",
    },
}

#: 必须先去代码块与行内反引号，否则文档里**作为例子**写的 `[[README]]`
#: 会被当成真链接。本库第一版就栽在这——报了 17 个"悬空"，**全是假阳性**。
FENCE = re.compile(r"^[ \t]*`{3,}.*?^[ \t]*`{3,}[ \t]*$", re.S | re.M)
INLINE = re.compile(r"`[^`\n]*`")
#: 兼容 markdown 表格里的转义竖线：`[[A/README\|打开]]`
WIKILINK = re.compile(r"\[\[([^\]\|]+)(?:\|[^\]]*)?\]\]")


def strip_code(text: str) -> str:
    return INLINE.sub("", FENCE.sub("", text))


def is_data(rel: str, rules) -> bool:
    """规则按**连续路径段**匹配，所以 ``vault/papers`` 能命中
    ``a/vault/papers/x.md``，``cache`` 能命中任意层级下的 ``cache/``。"""
    parts = rel.split("/")
    for r in rules:
        rp = r.split("/")
        n = len(rp)
        if any(parts[i:i + n] == rp for i in range(len(parts) - n + 1)):
            return True
    return False


def resolve(target: str, vault: Path, md_by_path: dict, by_stem: dict, cur_dir: str):
    """把 `[[X]]` 解析成 vault 相对路径。返回 ``(path | None, 原因)``。

    解析顺序照 Obsidian 的来：先按路径，再按 vault 级 basename。
    basename 命中多个 = **歧义**，这本身就是一条要报的问题（同名撞车）。
    """
    t = target.strip().rstrip("\\").strip()
    t = t.split("#")[0].strip()          # 去掉 [[X#小标题]] 的锚
    if not t:
        return None, "空链接"

    # 1) 按路径：vault 相对 与 相对当前文件目录，两种都试；补不补 .md 都试
    variants = []
    for base in (t, posixpath.normpath(posixpath.join(cur_dir, t))):
        variants += [base, base + ".md"]
    for v in variants:
        if v in md_by_path:
            return v, None
        if (vault / v).is_file():        # 附件（图片 / PDF）也算解析成功
            return v, None

    # 2) Obsidian 的 vault 级 basename 解析
    stem = posixpath.basename(t)
    if stem.endswith(".md"):
        stem = stem[:-3]
    hits = by_stem.get(stem, [])
    if len(hits) == 1:
        return hits[0], None
    if len(hits) > 1:
        return None, f"歧义（{len(hits)} 个同名：{', '.join(sorted(hits)[:3])}…）"
    return None, "悬空"


def main() -> int:
    ap = argparse.ArgumentParser()
    ap.add_argument("--root", default=CONFIG["root_md"], help="树根（vault 相对）")
    ap.add_argument("--vault", default=".", help="库根目录")
    ap.add_argument("--data", action="append", default=[],
                    help="额外的数据目录（可重复），如 --data vault/papers")
    args = ap.parse_args()

    rules = CONFIG["data_rules"] + args.data
    vault = Path(args.vault).resolve()
    md_by_path = {p.relative_to(vault).as_posix(): p for p in vault.rglob("*.md")}

    by_stem: dict[str, list[str]] = {}
    for rel in md_by_path:
        stem = posixpath.basename(rel)[:-3] if rel.endswith(".md") else rel
        by_stem.setdefault(stem, []).append(rel)

    graph: dict[str, set[str]] = {rel: set() for rel in md_by_path}
    dangling, ambiguous = [], []
    readme_raw: list[tuple[str, str]] = []

    for rel, path in md_by_path.items():
        if is_data(rel, rules):
            continue                      # 数据文件的链接不算树的一部分，也省掉读取
        raw = path.read_text(encoding="utf-8", errors="replace")
        if posixpath.basename(rel) in CONFIG["readme_names"]:
            # 注意用**原文**：下面要按字面找路径，剥了反引号就找不到了
            readme_raw.append((posixpath.dirname(rel), raw))
        for m in WIKILINK.finditer(strip_code(raw)):
            tgt, why = resolve(m.group(1), vault, md_by_path, by_stem,
                               posixpath.dirname(rel))
            if tgt:
                graph[rel].add(tgt)
            else:
                (dangling if why == "悬空" else ambiguous).append(
                    f"{rel} → [[{m.group(1)}]]（{why}）"
                )

    # ---- 可达性：从根 BFS ----
    if args.root not in md_by_path:
        print(f"!! 根 {args.root} 不在库里，无法算可达性", file=sys.stderr)
        return 1

    reachable, dq = {args.root}, deque([args.root])
    while dq:
        for nxt in graph.get(dq.popleft(), ()):
            if nxt not in reachable:
                reachable.add(nxt)
                dq.append(nxt)

    # 第二种边：**被 README 以纯文本路径提到**也算"树知道它"。
    # 为什么需要：根 README 的「散件」与「不在树上的目录」两节就是**列而不链**
    # （`CLAUDE.md`、`.claude/`），只认 [[双链]] 会把它们全报成孤儿。
    # 这个检查问的是"**有没有人知道它**"，不是"有没有人链它"。
    #
    # ⚠️ 匹配要试**两种**写法，缺一种就会造出一墙假孤儿（本库两种都踩过）：
    #   1. **相对那份 README 自己所在目录**的路径——README 写在文件夹内部，
    #      列同层文件时用裸名。拿库根相对路径去比，7 份根 md 全报孤儿。
    #   2. **裸文件名**——README 用子标题分组时（`### rl/` 下面是 `PPO.md`），
    #      连目录前缀都不写。只用第 1 种，`testing_note/` 的 33 篇全报孤儿。
    #      （⚠️ `testing_note/` 已于 2026-09-20 搬出本库，这里记的是当时的实测。）
    doc_nodes = {
        r for r in md_by_path
        if not is_data(r, rules) and r not in CONFIG["exempt"]
    }
    for r in doc_nodes:
        if r in reachable:
            continue
        base = posixpath.basename(r)
        for d, raw in readme_raw:
            if (posixpath.relpath(r, d) if d else r) in raw or base in raw:
                reachable.add(r)
                break

    orphans = sorted(doc_nodes - reachable)

    # ---- README 覆盖 ----
    missing_readme, tops = [], {}
    for r in doc_nodes:
        if "/" in r:
            tops.setdefault(r.split("/")[0], []).append(r)
    for top, files in sorted(tops.items()):
        if top in CONFIG["no_readme_ok"]:
            continue
        names = {posixpath.basename(f) for f in files}
        if not (names & CONFIG["readme_names"]):
            missing_readme.append(f"{top}/（含 {len(files)} 份 md，但没有 README）")

    # ---- 顺手查两张豁免表有没有变成空话 ----
    dead_exempt = [k for k in CONFIG["exempt"] if k not in md_by_path]
    dead_no_readme = [k for k in CONFIG["no_readme_ok"]
                      if not (vault / k).is_dir()]

    # ---- 报告 ----
    n_data = len(md_by_path) - len(doc_nodes)
    print(f"md 总数：{len(md_by_path)}（文档 {len(doc_nodes)} / 数据与豁免 {n_data}）")
    print(f"从 {args.root} 可达：{len(reachable & doc_nodes)} / {len(doc_nodes)}")

    ok = True
    for title, items in (
        ("悬空双链", dangling),
        ("歧义双链（同名撞车）", ambiguous),
        ("孤儿 md —— 从根走不到", orphans),
        ("缺 README 的文件夹", missing_readme),
    ):
        if items:
            ok = False
            print(f"\n[{title}] {len(items)} 处")
            for x in items[:30]:
                print("   -", x)
            if len(items) > 30:
                print(f"   …另有 {len(items) - 30} 处")

    for label, dead in (("豁免表", dead_exempt), ("直辖登记表", dead_no_readme)):
        if dead:
            print(f"\n[{label}已失效] {len(dead)} 条指向不存在的路径，可以删了")
            for x in dead:
                print("   -", x)

    print("\n树是完整的" if ok else "\n树有缺口——上面每一处都是某个新会话会走进的死胡同")
    return 0 if ok else 1


if __name__ == "__main__":
    sys.exit(main())
```

> **跑起来第一件事通常是给 `data_rules` 加条目。** 孤儿列表里有几百条时，别急着补链接——先问"这些是文档还是数据"。**把数据划出去，剩下的才是真的孤儿。**

## 6. Skill 化：把约定变成步骤

这套设施配**两个 skill**，均按 zhanlu / Claude 的 skill 结构（`skill/<name>/SKILL.md`）安装。

**当前项目（`移动云杯`）里它们装在 `.zhanlu/skill/` 下，一个管索引树、一个管日志：**

| skill | 管什么 | 触发时机 | 安装位置 |
|---|---|---|---|
| **`folder-readme`** | 索引树——根/子 README、单一索引、向上传播、自检，**并区分计划/数据文件（浏览只读计划文件）** | 建/改 README、回写索引、检查/自检树 | `.zhanlu/skill/folder-readme/` |
| **`research-log`** | 日志——一天一篇、扩写不新建、发现问题三段式 | 记录 / 写日志 / 总结 / 归档 | `.zhanlu/skill/research-log/` |

> **适配说明**：`research-log` 源版是写给某 Obsidian 库（写死 `D:\大学编程学习\obsidian`）；
> 本项目的 `research-log` skill 已适配为「当前项目」版——日志落在项目根 `日志/`、回写到当前项目的 `README.md`，不再写死库路径。
> 源文件与手册一起归档在 `科研设施/`（见文末）。

**skill 与"写进文档的约定"的区别只有一个，但它决定成败**：

| 写法 | 结果 |
|---|---|
| "改动之后要回写 README" | **期望** → 会被跳过 |
| "**回写根 README**（硬要求，**先做这一步再报告**）" | **步骤** → 不会 |

> **期望会被跳过，步骤不会——因为它挂在了一个必然发生的动作（报告）前面。**

### skill 的三段骨架

1. **frontmatter 的 `description`** —— 决定**什么时候触发**。要覆盖全部适用对象，并以那句"改动完成后逐层向上回写，直到根"收尾。
2. **正文按「你改了什么 → 该写什么」组织**，不要按"本文件有哪些章节"组织。前者是操作，后者是目录——后者没人会在动手时读。
3. **末尾留一个「维护这份 skill」段**：写清"想调 X 行为 → 改第 N 节"。否则下次没人敢改它。

### ⚠️ 改 skill 时最容易犯的错

**表格与它自己的说明文字是一对，不要插进中间。** 本库实测：把新章节插在表格与其 `**回写时**：` 说明之间，切开了一对。**改完把那一节连着读一遍**，比"看 diff 没错"可靠。

## 7. 入口那一句话（最容易漏，**漏了全废**）

树是给"新会话"用的，而**新会话不会自己去找树**。

项目根必须有一个**每次会话必读**的文件（`CLAUDE.md` / `AGENTS.md` / `.cursorrules`），里面**至少**要有四条：

1. **一张表**：有哪几块、各自是什么、哪块可写哪块只读
2. **一句硬要求**：「动手前先读那个文件夹的 README」
3. **一句回写要求**：「改动之后回写，并向上传播到根」
4. **撞车警告**（仅当同名时）

> **硬规则：浏览项目只读计划文件。** 入口表把每块标成「计划 / 数据」；读 README、日志、规划、经验这类**计划文件**进上下文，向量/聚类产物/语料这类**数据文件**不读（要么可重建，要么该由脚本读）。少了第 1、2 条，树是装饰品；**不区分计划/数据，浏览就被数据文件灌满。**

少了第 1、2 条，树是装饰品。

## 8. 分档：**别一上来就全上**

| 设施 | S 档<br>（<20 文件，一个人） | M 档<br>（多文件夹，有脚本） | L 档<br>（跨月，多数据源 / 多长任务） |
|---|---|---|---|
| 根 README + 子 README | ✅ | ✅ | ✅ |
| §5 自检脚本 | ✅ | ✅ | ✅ |
| `CLAUDE.md` 入口那一句 | ✅ | ✅ | ✅ |
| `日志/`（一天一篇） | 可选 | ✅ | ✅ |
| skill 化（§6） | ❌ | ✅ | ✅ |
| 机器生成区（下节） | ❌ | 可选 | ✅ |
| `STATUS` / `ROADMAP` 分开 | ❌ | 可选 | ✅ |

> **唯一的铁律：不要为了"以后可能需要"而加设施。**
> 等它开始腐了再加，也不晚——加早了只有一个后果：**没人维护的多余文件**，而那本身就是新的腐化源。

### 什么时候该上机器生成区

当清单里**某一列是"磁盘上有什么"**的时候。

**手抄的文件列表就是派生物**——它和忘记刷新的构建产物是同一个病：

> **派生物不会自己跟着真相源走，而且没有任何东西会报警。**

做法（本库已验证）：

```
<!-- AUTO:END -->     ← 这一行**以上**整体重写，**以下**永不动
```

1. 上面是生成区（文件清单、计数、日期），下面是手写区（用途、判据、为什么）
2. ⚠️ **必须按整行匹配**，不能用 `str.find`——生成的正文里会引用这个标记**本身**（本库的「使用说明」页就在教这条规则），按子串找会**截断文件并反复拼接**
3. **生成脚本本身也是产物，必须进「刷新链」**：新增任何产物，都要在同一次改动里加进刷新脚本——**漏一个 = 它永久不刷新**，只会一次次累积旧口径

## 9. 一次搭建的 checklist

- [ ] **1.** 定 `{{规模档}}`（§8），**砍掉本档不需要的设施**
- [ ] **2.** 定**边界**：哪些目录是数据 / 外部（不进树、写进自检的 `data_rules`），哪些不可写
- [ ] **3.** 建根 `README.md`（§2 模板），先只填 §1 那张表和 §4 阅读顺序
- [ ] **4.** 逐个建子 `README.md`（**同构**），§5 用纯文本路径指回根
- [ ] **5.** 放进 §5 的自检脚本，改 `CONFIG`，**跑一次**
- [ ] **6.** 写 `CLAUDE.md` 的入口四条（§7）
- [ ] **7.** 写 skill（§6，M / L 档）
- [ ] **8.** **再跑一次自检脚本**，全绿才算搭完

## 10. 踩过的坑（照抄可避）

每条：现象 → 根因 → 怎么办。

| # | 现象 | 根因 | 怎么办 |
|---|---|---|---|
| 1 | `"日志（5 篇）"` 实际 7 篇 | 同一个数抄了两遍 | §3 单一索引 |
| 2 | 「最后更新」手改，漏了第三处 | 它是派生物却手填 | §4，机器填或让脚本查 |
| 3 | 自检悬空 0、**全绿**，但承诺一半是空的 | 只查了悬空，**没查孤儿** | §5 第 2 条 |
| 4 | 自检打真实站点，带宽一抖就红 | 报的是"网线的问题"不是代码 | §5 铁律：**离线** |
| 5 | 所有 `README.md` 全同名（# 数它自己是没用的——**这个数一变，这一行就过期**） | 图省事全叫 README | §2 命名，**复刻时别抄** |
| 6 | "记得回写"没人记得 | 写成了**期望**不是**步骤** | §6 |
| 7 | 根 README 开始记叶子细节 | 它被当成了流水账 | §4 那条粗体 |
| 8 | 新建日志忘了回写根 → **日志从索引里消失** | 日志**没有自己的 README**，根是它唯一的索引 | §6，写成硬要求 |
| 9 | 自检报了 17 个"悬空"，**全是假阳性** | 正则没剥代码块与行内反引号，把**举例**当成了链接 | §5 脚本的 `strip_code` |
| 10 | 修好 #9 后在真实库上跑：4 条"悬空"里 **3 条是 skill 模板的占位符**，248 条"孤儿"里 **97 条是数据集** | 三条边界假设全是**想出来的**，没跑过 | §5：**先跑，再按报告修配置，别按想象修配置** |

⚠️ **下面两条不是"树"的坑，是「在这台机器上干活」的坑**——本库做审计时踩到、可复用，
**2026-09-20 从 `日志/2026-09-10` 的 §6 搬进来的**（那篇日志已随它所属的线搬走/删除）：

| # | 现象 | 根因 | 怎么办 |
|---|---|---|---|
| 11 | Git Bash 里 `curl -d '<中文 JSON>'` → 后端报 `There was an error parsing the body`（HTTP **400**）。**看着像后端的锅** | 命令行上的中文被转成 **GBK**，后端收到非法 UTF-8 | **改用文件体**：JSON 写成 **UTF-8 文件**，`curl --data-binary @body.json`。⚠️ 与本库另一条同源（GBK 会在 `print` 到一半时抛 `UnicodeEncodeError`，**前半截看着完全正常**） |
| 12 | `pkill -f xxx` 杀不掉 Windows 的 `.exe`：旧进程仍占着端口，新进程起不来（`error while attempting to bind`），**而请求全打到了旧进程上**，导致"环境变量没生效"这种**假归因** | `pkill` 按 POSIX 进程名匹配，与 Windows 的 `.exe` 对不上 | `taskkill //PID <pid> //F`，并**先** `netstat -ano \| grep :8000` 确认端口已释放，**再**起新的 |

---

## 11. 附录：skill 种子全文（让本手册自洽）

> **这一节就是种子**：把下面三份完整落盘，这本手册就足以在**任意新项目**复现整套设施——
> 照 §9 搭树，再按 11.0 的映射把三份 skill 文件落进 `{{项目根}}/.zhanlu/skill/`。
> 三份文件与「激活版」内容一致（`research-log` 已是**当前项目适配版**，不写死库路径）。

### 11.0 种子 → 安装映射

| 本手册里的 | 落盘位置 | 是什么 |
|---|---|---|
| §11.1 | `{{项目根}}/.zhanlu/skill/folder-readme/SKILL.md` | 索引树 skill |
| §11.2 | `{{项目根}}/.zhanlu/skill/research-log/SKILL.md` | 日志 skill |
| §11.3 | `{{项目根}}/.zhanlu/skill/research-log/templates/日报.md` | 日志模板 |

### 11.1 folder-readme · 索引树 skill

````markdown
---
name: folder-readme
description: 维护项目的「索引树」——根/子 README、单一索引、向上传播、自检脚本，并区分**计划文件**（日志/经验/规划，浏览时只读这类）与**数据文件**（产物/语料，不读）。当用户说「建/改 README」「回写索引」「这个文件该不该进 README」「检查/自检索引树」「新建会话该读什么」「这个文件夹放什么」「浏览项目该读哪些文件」「这个是计划还是数据文件」时触发。**每次改动（新建/删除/改名/改用途/新建日志）完成后，顺着树向上回写子 README 与根 README，终点是根；改完若有自检脚本，跑一遍直到全绿再报告。**
---

# 索引树维护（folder-readme）

给当前项目维护一棵「从根 README 出发就能读全」的索引树。不是"文档写得好"，是对三个具体失败模式的回答：**静默腐化**（清单落后于磁盘、没人报警）、**入口缺失**（新会话不知道从哪进）、**靠人记住的约定失效**。

## 一、这棵树的形状

三层，节点只有两种类型；**子 README 之间必须同构，根必须不同构**（根是地图，不含状态、不含叶子细节）。

```
根 README.md          ← 总目录。回答"有哪些块"
  ├── 子 README.md    ← 每块一份。回答"这块有什么、现在到哪了"
  │     └── 叶子文件   ← 见下方「计划文件 / 数据文件」两类
  └── 根目录散件       ← 不属任何块的文件，也在根 README 里登记
```

### 叶子文件分两类：计划文件 / 数据文件

**任何 md（和文件）先归到两类之一，README 按这个分，浏览也按这个读。**

| 类型 | 是什么 | 谁来读 | 例子 |
|---|---|---|---|
| **计划文件** | 记录**日志、经验、规划**、决策、状态的文件 | **新会话必须读**，浏览项目只读这一类 | `日志/`、`经验/`、`规划/`、设计文档、`_index.md`、README |
| **数据文件** | 语料、产物、生成数据、构建结果——量大、可重建 | **新会话不读**（占 token 且失真） | 向量 `.npz`、聚类 json、数据集、缓存 |

> **每次浏览项目只读计划文件。** 数据文件不进上下文：它们要么能重建、要么本该由脚本/工具读，人读只会灌 token 又被过大文件带偏。

⚠️ **命名**：子 README 不要也叫 `README.md`——同一个项目里所有 README 同名会造成双链歧义。复刻时子 README 叫 `_index.md` 或 `README-<folder>.md`。

⚠️ **「直辖」文件夹**：没有子 README 的文件夹（如 `日志/`），由根 README 直接管辖——根里给它一个专属节，并**明确写**"没有子 README"。否则新会话会去找它。

## 二、单一索引原则

> **同一个事实只留一份，留在最不容易腐的那一层。**

判据：**问"这个数变了的时候，谁会知道？"** 如果答案是"没人"，就把它挪到唯一的那一处。典型：

| 数字 | 该放哪 |
|---|---|
| 一个文件夹下有多少**计划文件** | **那个文件夹的 README §3**，或根里管辖它的那节 |
| 一个文件夹下有多少**数据文件** | **不逐条列**——只在 §3 记为"数据 ×N，可重建"，别把几千个产物读进索引 |
| 全库总篇数 / 外部源可用性 | **只写一处**，别处只引用 |
| 改了多少、这次做了什么 | **哪里都不放**（根是地图，不是流水账） |

## 三、你改了什么 → 该写哪里（本 skill 的核心，先做再报告）

**规则：改动任何 md 之后，顺着树往上写，终点是根 README。由执行这次改动的那次会话写。**

| 你改了什么 | 子 README | 根 README |
|---|---|---|
| 新建一个**计划文件** | §3 加一行 | 只改 §1 那一行的「最后更新」 |
| 删除 / 改名一个计划文件 | §3 删行 / 改行 | 同上 |
| 数据文件生成 / 体积变大 | **不动**（§3 那个"数据 ×N"一般不逐条登记） | 不动 |
| 改了工作状态、判据 | §2 改 | **不动** |
| 文件夹的**用途定位变了** | §1 改 | 改 §1 那一行的「干什么」 |
| 新建一篇日志 | — | 根里那一节加一行 |
| 根目录新建 / 删除散件 | — | §3 加 / 删一行 |
| 只改错别字、只动排版 | **不动** | **不动** |

> **根只吃「那一行」。它是地图，不是流水账。** 一旦开始记"这次改了什么"，根就会变成那种"会让人重做已经做完的事"的失真清单。
> **期望会被跳过，步骤不会**——把回写写成本 skill 硬要求、挂在"报告"这个必然动作前面。

## 四、自检脚本

树的失败模式是**静默**——没有东西会报错，它只是慢慢变得不全。所以自检必须**离线、秒级、确定性**，且查三件事：

1. **悬空双链 = 0**（`[[X]]` 指向不存在的文件）
2. **无孤儿 md**（每个文档都能从根走到 ← 承诺的本体；**只查悬空不查孤儿 = 假绿**）
3. **README 覆盖**（每个含 md 的一级文件夹都有 README）

⚠️ 跑起来第一件事通常是给 `data_rules` 加条目——把**数据**（语料、缓存、构建产物、skill 模板）划出去，剩下的才是真的孤儿。**先跑，再按报告修配置，别按想象修配置。**

skill 与模板通常**不在树上**（声明进根 README §5「不在树上的目录」，并在自检里豁免）。

## 五、README 模板

**根 README**（五节，`tags: [索引, 说明书]`）：

```markdown
# <项目名> · 总目录
## 1. 索引树          ← 表格：文件夹/干什么/它的README/最后更新
## 2. <直辖文件夹的索引>  ← 没有子 README 的文件夹
## 3. 根目录的散件
## 4. 建议阅读顺序       ← 决定新会话的头五分钟；**只列计划文件，不列数据**
## 5. 不在树上的目录      ← 每项写"为什么不列"
## 6. 维护本文件（树形回写） ← §三的传播表，本文件那一列
```

**子 README**（同构，`_index.md`）：

```markdown
## 1. 这个文件夹是什么
## 2. 当前工作状态      ← 问"现在到哪了"只读这一节
## 3. 文件清单          ← 分两段：**计划文件**（逐条列，且是唯一要读的）
                        ←          **数据文件**（记为"数据 ×N，可重建，不读"）
## 4. 建议阅读顺序       ← **只列计划文件**——新会话按这个读
## 5. 相关              ← 含上位指针（指回根，用纯文本路径）
```

⚠️ §3 与 §4 是配套的：**§3 标出"哪些是计划文件"，§4 就只排它们**。数据文件不出现 §4——否则"浏览只读计划文件"成了一句空话，又被几千个产物拖回原点。

## 六、入口那一句话（最容易漏，漏了全废）

树是给"新会话"用的，而**新会话不会自己去找树**。项目根必须有 `CLAUDE.md` / `AGENTS.md` / `.cursorrules`，至少四条：

1. **一张表**：有哪几块、各自是什么、哪块可写哪块只读
2. **一句硬要求**：「动手前先读那个文件夹的 README」
3. **一句回写要求**：「改动之后回写，并向上传播到根」
4. **撞车警告**（仅当同名时）

> **硬规则：浏览项目只读计划文件。** 入口表把每块标成「计划 / 数据」；读 README、日志、规划、经验这类**计划文件**进上下文，向量/聚类产物/语料这类**数据文件**不读（要么可重建，要么该由脚本读）。少了第 1、2 条，树是装饰品；**不区分计划/数据，浏览就被数据文件灌满。**

## 七、维护这份 skill

- 想调「回写表」→ 改 **§三** 那张表
- 想调自检的三项检查 → 改 **§四**
- 想调 README 模板 → 改 **§五**，模板里「计划 / 数据」两份清单是一对，不要拆开
- 想调「浏览只读计划文件」的判定（什么算计划文件）→ 改 **§一** 那张两分表
- 改了表格要和它的说明文字连着读一遍（一对，不要插进中间），比看 diff 可靠
````

### 11.2 research-log · 日志 skill（当前项目适配版）

````markdown
---
name: research-log
description: 在项目根的 `日志/` 目录下写研究日志。当用户说「记录一下」「写日志」「今天的实验总结一下」「写篇总结」「归档一下」时触发。**动笔前先查当天有没有日志——有就扩写那一篇，不要再开新文件。**产出 `日志/YYYY-MM-DD-标题.md`，写完后**先回写项目根 `README.md`** 再报告。
---

# 研究日志（research-log）

给当前项目写研究日志。日志留在项目根的 `日志/` 目录，**平铺、不建子目录**。

日志的价值不仅在"我今天干了啥"，也在为什么这么干，但最重要的是**简洁**。

## 一、动笔前先做四件事

1. **拿今天的日期**（格式 `YYYY-MM-DD`，例如 `2026-09-22`）。
2. **查今天是否已经记过**：列 `日志/` 里 `YYYY-MM-DD-*.md` 的今天那份。
   ⚠️ **有 → 这是「扩写」，不是「新建」**，按 §二「同一天只留一篇」办。
   **这一查就能避免同一天开出第二篇**，务必先做。
3. **读上一篇**：`日志/` 里按文件名倒数第一篇，读它的结论和它留下的 `## 待完成`。
   （若第 2 步查出今天已经有日志，那"上一篇"就是**今天这一篇**——直接读它、接着往下写。）
   - 上一篇的待办这次做完了 → 在正文里交代结果；没做 → 原样带进新日志的待完成。

## 二、存放与命名

- 目录：`日志/`，**平铺，不建子目录**
- 文件名：`YYYY-MM-DD-标题.md`
- 标题用 `·` 或 `与` 连接两个概念，**文件名内不加空格**
- 参照已有文件：`2026-09-20-D3分块向量化·跨会三态.md`

### 同一天只留一篇

**一天一篇，不是一件事一篇。** 同一天里又干出新的东西、又冒出新的坑，**要落进已经存在的那一篇**。

动手前必查：今天有没有 `日志/YYYY-MM-DD-*.md`。

**已经有一篇 → 扩写它**，具体怎么做：

| 动作 | 要求 |
|---|---|
| 新内容往哪放 | **按主题并入对应章节**。能并进去就并，**不要在文末堆一段「补充」**|
| 章节编号 | 并入后**重排 `## 1.` … `## N.`**，不要出现两个 `## 3.` |
| `## 待完成` | 原来那条这次做掉了 → **勾掉 `[x]` 或改写**。别让同一件事既在"待完成"里躺着、正文里又说已完成 |
| `## 发现的问题` | 新坑**按严重程度插进去**，不是追加到末尾 |
| `tags` | 新内容确实引入了新主题就补，但**别堆** |
| 顶部 `> 承接` | **不动**——它承接的是"那一天"，不是"那一段" |

**没有 → 才新建**，按上面的命名规则。

### ⚠️ 扩写时不要改文件名

标题覆盖的是**那一天的整条线**，所以名字一旦定下就**别再动**。

- 新内容超出旧标题的覆盖范围了？**接受它**——一天之内的事本来就是一根线上长出来的。
- **真要改名，必须是用户明确要求。** 用户没提就不改，改了会打断所有指向它的 `[[双链]]`（若在用 Obsidian）。

> **例外（要问，不要自己决定）**：同一天里确实是**两条不相干的线**。但**默认是合成一篇**，分不分**先问用户**。

## 三、正文

从 `templates/日报.md` 起手。章节顺序：

| 段落 | 内容 | 必要性 |
|---|---|---|
| `**环境**` 表 | 项目 / 机器 / 工具版本 / 模型 / 数据规模 | 涉及具体环境时必写 |
| `## 1.` … `## N.` | 按主题分的正文，**不是按时间流水账** | 必写 |
| `## 发现的问题` | 见 §四 | 有问题就写，没问题可省 |
| `## 待完成` | `- [ ]` 复选框 | 必写 |
| `## 相关` | 关联文件 / `[[双链]]`（非 Obsidian 项目用纯文本路径） | 必写 |

## 四、写「发现的问题」的规矩

这是整篇日志最值钱的部分。铁律：

1. **按严重程度排，最严重的标 `【最严重】`**。
2. **三段式**：现象 → 根因 → 证据。证据要贴**原始输出**（报错原文、命令、SQL、日志行），不要只给转述。
3. **凡有数字，必是实测的**（`166 MB`、`96%`）——这种具体度是这些日志的底子。**不确定的数字不要写，标 `<!-- 待确认 -->`**。

## 五、语言风格

- 最重要的是**简洁**！
- 中文正文，代码/路径/命令/报错原文保持原样
- **表格优先**：并列的项 >3 个就上表格，不要写成逗号长句
- 章节标题是**结论**不是**动作**
- 允许并鼓励用粗体标出关键判断
- 长度不设上限，但**不要为了长而灌水**；一句话能说完就别写三段

## 六、禁止

- **不要编造**任何数字、路径、命令输出、报错原文。对话里没有的就不写，或标 `<!-- 待确认 -->`
- **不要写成"我做了 A，然后做了 B"的流水账** —— 按主题重组
- **不要把对话原文贴进来** —— 是提炼，不是转录
- 不要建子目录、不要改已有日志的文件名

## 七、写完之后

1. **回写项目根 `README.md`**（**硬要求，先做这一步再报告**）——
   **新建**一篇：在 README 里登记日志的那一节（或 `日志/` 的索引）加一行；
   **扩写**今天已有的那篇：改那一行的「讲什么」。
   > ⚠️ **`日志/` 没有自己的 README——项目根的 README 是它唯一的索引。** 不回写，新会话从根 README 出发就找不到这篇日志，整棵索引树的承诺就断了。规则见 `folder-readme` skill §三「向上传播」。
2. 告诉用户：文件落在哪、标题是什么，**并说清这次是「新建」还是「扩写了今天已有的那一篇」**——扩写的话，报出**新增/改动了哪几节**（用户要能一眼看出旧内容有没有被动过）。
3. 附一句本次对话里**还没解决**的事（如果 `## 待完成` 里写了，提醒他那就是下次的起点）。
4. 如果这篇和更早的日志构成一条线，提示他可以补一条关联过去。

---

## 维护这份 skill

- 想改目录 / 命名规则 → 改 **§二**
- 想改正文结构 / 环境表 → 改 **§三** 与 `templates/日报.md`
- 想改「发现的问题」铁律 → 改 **§四**
- 想改回写要求 → 改 **§七**
````

### 11.3 research-log/templates/日报.md

````markdown
---
tags: [实验, 总结]
---

# {{YYYY-MM-DD}} {{标题}}

> 承接 [[{{上一篇}}]]。{{上次到哪了 → 这次要解决什么，一到两句}}

**环境**

| 项 | 值 |
|---|---|
| {{项目 / 机器}} | `{{路径}}` |
| {{工具或模型}} | {{名称与版本}} |
| {{数据规模}} | {{N 篇 / N 条}} |

---

## 1. {{结论性标题，例如"为什么这个插件装了却没用"}}

{{结论先行，再给证据。表格、命令、原始输出都可以贴。}}

## 2. {{……}}

{{按主题分章，不按时间。}}

---

## 发现的问题

> 按严重程度排，最严重的在前。

### 1. 【最严重】{{问题一句话}}

{{现象}}

{{根因}}

```
{{原始报错 / 命令 / SQL / 日志行}}
```

**结论**：{{一句话}}

### 2. {{问题一句话}}

{{现象 → 根因 → 证据}}

## 待完成

- [ ] {{下一步动作}}
- [ ] {{……}}

## 相关
- {{上一篇的路径或 [[双链]]}}
- {{其它相关文件}}
````

